/**
 * Legacy App component — kept as a placeholder for future task reorganization.
 * Routing is now handled by react-router-dom via `router.tsx`.
 *
 * @deprecated This component is no longer mounted directly.
 * Its contents (settings panels, chat, etc.) will be migrated into
 * dedicated route pages in upcoming tasks.
 */
export function App(): JSX.Element {
  return <div />
}
