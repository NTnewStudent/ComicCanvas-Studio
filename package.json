{
  "name": "comic-canvas-studio",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first AIGC comic-drama canvas and agent orchestration desktop client.",
  "workspaces": [
    "shared",
    "desktop"
  ],
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "dev": "echo \"see desktop/ for app dev script (to be scaffolded in M1)\"",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "verify:repo": "node scripts/verify-repo.mjs",
    "release:dry-run": "node scripts/release-dry-run.mjs",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run build && npm run verify:repo"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "^2.1.8"
  }
}
