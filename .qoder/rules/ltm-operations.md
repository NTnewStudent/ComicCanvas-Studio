---
description: "LTM is disabled for this project"
alwaysApply: true
---

## LTM Disabled

不要运行 `ltm/bin/ltm.py`，不要读取 `ltm/runtime/active-context.json`，也不要
创建 LTM checkpoint。

当前任务状态以 `specs/`、`docs/progress/`、git 状态和用户最新指令为准。
