---
description: Electron / Node 主进程与 preload 编码规范
globs: desktop/src/main/**,desktop/src/preload/**
alwaysApply: false
---

# Electron / Node 主进程编码规范

> 适用 `desktop/src/main/**` 与 `desktop/src/preload/**`。无 NestJS / 无 Redis。

## 进程模型与安全（强制）

- `BrowserWindow.webPreferences`：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`。
- 渲染层只能通过 `preload` 用 `contextBridge.exposeInMainWorld` 暴露白名单 API。
- 禁止把整个 `ipcRenderer` 暴露给渲染层。
- 所有 `ipcMain.handle` 入参用 Zod 校验后再进 service。
- 自定义资产协议（`cc-asset://`）用 `protocol.handle` 注册，做路径越界校验。

## IPC 契约

- 通道名与负载类型定义在 `shared/ipc.ts`。
- 命名：`domain.action`（如 `canvas.runNode`、`job.subscribe`）。
- 请求走 `invoke/handle`，推送走 `webContents.send`。

## 任务队列

- 队列状态持久化到 SQLite `jobs` 表。
- 入队入口 ≤1s 返回票据；绝不在入口同步等待模型。
- 终态事件每个 jobId 恰好一次。

## 密钥与隐私

- API key 走 OS 安全存储，不落明文、不进日志、不进 LTM。
- 错误日志做密钥红化。

## 分层

- handler 薄、service 厚、DB 走仓储层。
- 纯逻辑放可测纯函数模块。
